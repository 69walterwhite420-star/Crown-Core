[profile.default]
solc_version = "0.8.30"
optimizer = true
optimizer_runs = 200
# No trailing CBOR metadata: the byte-exact runtime code is what the opcode
# lint walks and what freezes on chain.
cbor_metadata = false
bytecode_hash = "none"
src = "src"
test = "test"
out = "out"
libs = ["lib"]
