# Crown Core — CLAUDE.md

Этот репозиторий содержит **только ядро**. Ядро — два артефакта и больше ничего:

1. **`splitter`** — неизменяемый контракт 97/3 на внешней сети. Делит донат в транзакции донора. Баланса не имеет.
2. **`crown-index`** — канистра ICP. Читает `Settled` от пиннутого сплиттера, сворачивает в книгу репутации, отдаёт её на чтение. Денег нет, ключей нет, подписи нет, update-методов нет.

Всё остальное — игры, эскроу, фабрика, фронт, бэкенд, модерация, косметика — **не здесь**. См. `docs/post-core.md`.

Этот файл читается каждую сессию. Держи коротким.

## Незыблемые принципы

1. **Ядро только считает.** `book = fold(reduce, empty, settlements)`. Чистая, детерминированная, версионированная функция без I/O.
2. **Репутация ≡ честно уплаченный доллар.** 1 USD gross, дошедший до стримера через сплиттер = 1 rep. Линейно, без decay, монотонно, непередаваемо, per-wallet, локально для стримера. Дельт нет. Дебета нет. Возврата нет.
3. **Единственный вход в книгу — пиннутый сплиттер.** `Settled` от любого другого адреса игнорируется. Не «любой контракт с событием `Settled`», не «прислал 3%».
4. **Сплиттер не держит денег.** Средства идут донор → стример и донор → казна двумя переводами внутри одной транзакции. Контракт не является промежуточным владельцем ни на один слот.
5. **Индексатор не знает про деньги.** Он не знает `feeBps`, не знает адрес казны, не проверяет 3%. Он доверяет **адресу**, а не числам в событии. Честность чисел обеспечена неизменяемостью контракта.
6. **Границы — компилятором, не договорённостью.** `crown-reduce` — крейт с нулём зависимостей. Он физически не может сделать I/O. Это проверяется в CI, а не в ревью.
7. **Чтение чейна — только через RPC-канистры под NNS.** `EVM RPC` (`7hfb6-caaaa-aaaar-qadga-cai`), `SOL RPC` (`tghme-zyaaa-aaaar-qarca-cai`), с мульти-провайдерным консенсусом. На mainnet — только `Default`-источники. Свои URL и API-ключи запрещены.
8. **Минимализм = безопасность.** У каждой строки есть текущий потребитель. Никакой обобщённости «на будущее». Сомневаешься между «добавить» и «удалить» — удаляй.

Расхождение кода с принципами или с `docs/` = баг. Не «решаем на месте»: правь код; если неверен принцип — сначала правь доку.

## Документы

- `docs/core-spec.md` — что такое ядро, целиком. **Читать перед любой работой.**
- `docs/build-plan.md` — этапы и текущий статус. **Читать в начале сессии.**
- `docs/standards.md` — код, границы, тесты, конфиг. **Читать перед реализацией.**
- `docs/post-core.md` — всё, чего здесь нет и почему. **Читать, когда тянет что-то добавить.**

## Структура репозитория

```
crown-core/
├─ CLAUDE.md
├─ docs/
├─ reduce/                 # КРЕЙТ БЕЗ ЗАВИСИМОСТЕЙ. Чистая свёртка.
│  └─ src/{lib,event,book,reduce}.rs
├─ index/                  # КАНИСТРА. Только query в Candid.
│  └─ src/{lib,api,cursor,certify,source/{mod,solana,evm}}.rs
├─ contracts/
│  ├─ solana/              # Anchor. Upgrade authority сожжён.
│  └─ evm/                 # Solidity. Ни owner, ни proxy, ни selfdestruct.
└─ config/
   ├─ testnet.toml
   └─ mainnet.toml
```

Зависимости строго в одну сторону: `index → reduce`. Обратной нет. `reduce` не знает про ICP, про Solana, про EVM, про Candid.

## Как работать

1. Прочитай `docs/build-plan.md`, определи текущий этап (первый не-`DONE`).
2. Работай строго по нему. Не забегай вперёд.
3. Этап не закрыт, пока Definition of Done не выполнен и тесты зелёные.
4. По завершении — обнови статус, удали устаревшую доку тем же коммитом, только потом следующий этап.

## Гардрейлы (NEVER)

- Никогда не добавляй в `reduce` зависимость. Ни одну. `serde`, `candid`, `ic-cdk` — в `index`, конверсия на границе.
- Никогда не экспортируй `update`-метод из канистры. Ингест — внутренний таймер. В `.did` только `query`.
- Никогда не засчитывай `Settled` от адреса, отличного от пиннутого сплиттера этой сети.
- Никогда не давай ядру держать деньги, ключи, подписывать, резолвить исходы.
- Никогда не вводи дельты репутации, дебет, сгорание, decay.
- Никогда не пиши сплиттер так, чтобы он в какой-то момент владел токенами. Ноль баланса — инвариант, а не следствие.
- Никогда не оставляй в сплиттере owner, admin, upgrade authority, proxy, `delegatecall`, `selfdestruct`.
- Никогда не используй `Custom` RPC-источники в mainnet-профиле.
- Никогда не хардкодь сетевые значения. Только `config/`.
- Никогда не используй `unwrap`, `expect`, `panic!`, немаркированную арифметику в `reduce`.
- Никогда не добавляй код «на будущее» без текущего потребителя. Если хочется — это `docs/post-core.md`.

## Стек

- `reduce`: Rust, `#![no_std]`-совместимый по духу, zero deps, `#![forbid(unsafe_code)]`.
- `index`: Rust, `ic-cdk`, `ic-stable-structures`, клиенты RPC-канистр.
- `contracts/solana`: Anchor. `contracts/evm`: Solidity.
- Доки и проза — по-русски. Идентификаторы, комментарии, имена файлов кода — по-английски.
